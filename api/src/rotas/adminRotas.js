const router = require('express').Router(); const auth = require('../middleware/autenticacao'); const autorizar = require('../middleware/autorizacao'); const ctrl=require('../controladores/adminCtrl'); router.use(auth,autorizar('admin')); router.get('/dashboard',ctrl.dashboard); router.get('/metricas/saude',ctrl.metricasSaude); router.get('/moderacao/reportes',ctrl.moderacao); router.post('/nutricionistas/:id/verificar',ctrl.verificarNutricionista); router.post('/usuarios/:id/bloquear',ctrl.bloquearUsuario); module.exports = router;
