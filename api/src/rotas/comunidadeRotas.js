const router=require('express').Router(); const auth=require('../middleware/autenticacao'); const ctrl=require('../controladores/comunidadeCtrl'); const autorizar=require('../middleware/autorizacao'); router.get('/grupos',auth,ctrl.listarGrupos); router.post('/grupos',auth,autorizar('nutricionista','admin'),ctrl.criarGrupo); router.post('/grupos/:id/entrar',auth,ctrl.entrarGrupo); router.post('/grupos/:id/moderar',auth,autorizar('nutricionista','admin'),ctrl.moderarGrupo); router.post('/reportes',auth,ctrl.denunciar); module.exports=router;
