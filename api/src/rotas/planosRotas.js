const router=require('express').Router(); const auth=require('../middleware/autenticacao'); const autorizar=require('../middleware/autorizacao'); const ctrl=require('../controladores/planosCtrl');
const comunidade=require('../controladores/comunidadeCtrl');
router.get('/',ctrl.listarPlanos); router.get('/:id',ctrl.obterPlano); router.post('/',auth,ctrl.criarPlano); router.put('/:id',auth,ctrl.atualizarPlano); router.delete('/:id',auth,ctrl.removerPlano); router.post('/:id/receitas',auth,ctrl.adicionarReceita); router.delete('/:id/receitas/:receitaId',auth,ctrl.removerReceita); router.post('/:id/exercicios',auth,autorizar('nutricionista','admin'),ctrl.adicionarExercicio); router.post('/:id/seguir',auth,comunidade.seguirPlano); router.put('/:id/progresso',auth,comunidade.progressoPlano); module.exports=router;
