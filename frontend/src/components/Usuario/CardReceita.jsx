export default function CardReceita({ receita, aoCurtir }) { return <article className="card content-card"><div className="content-media" style={receita.foto_url ? { backgroundImage: `url(${receita.foto_url})` } : undefined}>{!receita.foto_url && <span>aptus / receita</span>}</div><p className="kicker">{receita.categoria || 'nutrição'}</p><h2>{receita.titulo}</h2><p>{receita.descricao}</p><div className="content-meta"><span>{receita.calorias || '-'} kcal</span><span>{receita.proteina || '-'}g proteína</span><button className="ghost" onClick={() => aoCurtir?.(receita)}>♡ {receita.curtidas || 0}</button></div></article>; }
